# Server-generated state (never commit)
.ftp-deploy-sync-state.json
.ftpquota
error_log
*.phpupgrader.*

# Local/editor cruft
.DS_Store
Thumbs.db
node_modules/

# Setup helpers and DB dumps are kept in /setup but never deployed.
# (They stay in the repo for reference; the deploy workflow excludes them.)
