# Copy this file to ".env" in the project root and fill in real values.
# ".env" is git-ignored and must NEVER be committed.

DB_HOST=localhost
DB_NAME=johnsonk_heisenbergstudios
DB_USER=johnsonk_datadev
DB_PASS=czwF~gOPkc4G

