# Deny all direct web access to this directory.
# Reports must be served through investments/equity_reports.php, which
# enforces login + access-level checks before streaming a file.
Require all denied
